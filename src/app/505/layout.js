import React from 'react'

export const metadata = {
  title: 'Error Occured At Server — Best Tosan',
  description: 'I craft delightful and scalable full-stack applications. I specialize in both front-end and back-end development, so I can craft amazing UI designs and write the API code and business logic that makes them work.',
  }

export default function layout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}
